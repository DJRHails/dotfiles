# setup.sh seeds ~/.git-template with prek, a cargo tool from the rust module.
rust
# setup.github.sh appends a `Host github.com` block to ~/.ssh/config, which is a
# symlink into the ssh module. Linking after that write backs the file aside
# (scripts/link.sh) and loses the block.
ssh
