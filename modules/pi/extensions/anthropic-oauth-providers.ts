import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function anthropicOauthProvidersDisabled(_pi: ExtensionAPI) {
  // Anthropic org access is configured as API-key providers in ~/.pi/agent/models.json.
}
