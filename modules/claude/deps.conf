# install.sh installs `ant` with install::go_tool, which needs the go toolchain.
go
# mcp.json launches the playwriter and context7 servers with `npx`.
node
# settings.json registers four hooks as `python3 .../hooks/*.py`.
python
